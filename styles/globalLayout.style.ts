import { StyleSheet } from "react-native";

const globalLayoutStyles = StyleSheet.create({
  safeAreaView: { flex: 1, backgroundColor: "white" },
  stackView: { flex: 1, backgroundColor: "white" },
});
export default globalLayoutStyles;
